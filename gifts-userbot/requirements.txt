# Юзербот-сервис покупки коллекционных подарков (MTProto, не Bot API).
# kurigram — поддерживаемый форк Pyrogram (тот же импорт `pyrogram`), совместим с
# новыми Python (3.13/3.14); оригинальный pyrogram 2.0.106 на 3.14 падает.
kurigram==2.2.23
# tgcrypto — опционально, ускоряет крипту (нужен C-компилятор; на Linux/Docker ок).
# tgcrypto

# HTTP-сервис для Go-бэка (внутренний API buy+deliver / каталог).
fastapi==0.115.0
uvicorn==0.30.6

# Portals Marketplace API — покупка/листинги подарков за TON.
# НЕ на PyPI: ставится из git (базовая либа архивирована сен-2025; при проблемах
# берём async-форк — уточним на де-риск-прогоне).
#   pip install git+https://github.com/bleach-hub/portalsmp.git
portalsmp @ git+https://github.com/bleach-hub/portalsmp.git

# TON-кошелёк юзербота (пополнение баланса Portals) — добавим на прогоне:
# tonutils
